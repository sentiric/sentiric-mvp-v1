{
  "name": "sentiric-mvp-v1-local-ai",
  "version": "1.0.0",
  "description": "Sentiric platformunun tamamen yerel LLM (Ollama) ve TTS (Piper) ile çalışan prototipi.",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm:start:gateway\" \"npm:start:worker\"",
    "start:gateway": "node gateway/gateway-server.js",
    "start:worker": "node worker/worker-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}